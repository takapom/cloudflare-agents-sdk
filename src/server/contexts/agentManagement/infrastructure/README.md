# Infrastructure

Concrete agent catalog adapters belong here.

Current implementation:

- `staticAgentRegistry.ts`

Cloudflare-specific or persistence-specific adapters should be added when the
agent catalog stops being static.
